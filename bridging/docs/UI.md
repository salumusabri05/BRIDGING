Modify this app UI , it is very bad though yeahis it an mvp thing 
there is no needto explain technicalities in the pages as the sers are just non code nerds , you just have to explain what the app does and ptoject in general to relevance in Tanzania
the camera should get landmarks and send them to url and be interprted and show back the result inthe required format as I instructed 

📱 Mobile App (React Native)
      │
      ▼
Camera → MediaPipe detects 21 landmarks
      │
      ▼
Extract landmark coordinates only
      │
      ▼
Send JSON → API
      │
      ▼
Backend loads .pkl model
      │
      ▼
Predict letter/gesture
      │
      ▼
Return result to mobile
theme color , sea blue
